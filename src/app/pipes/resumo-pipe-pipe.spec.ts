import { ResumoPipePipe } from './resumo-pipe-pipe';

describe('ResumoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ResumoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
